"""
Healthcare Knowledge Navigator — Preprocessing Module.

Handles parsing JATS XML documents, extracting structured sections,
cleaning text, and preparing data for chunking.

Components (to be implemented in Phase 1):
    - JATS XML parser (lxml + BeautifulSoup4)
    - Section extractor (title, abstract, body, references)
    - Text cleaner and normalizer
    - Metadata extractor (authors, DOI, journal, date)
"""
